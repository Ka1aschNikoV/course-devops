
FROM node:19

# Establish workdir
WORKDIR /usr/service1
COPY ./ /usr/service1
COPY run-log.log /shared/run-logs/run-log.log

# Save container creation time for uptime calculation of Debug Monitor
RUN echo "$(date +%s)" > /etc/container_creation_time.txt
